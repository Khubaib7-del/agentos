---
description: Queue a review note for the agent's next review pass
allowed-tools: Bash(agentos note:*)
---

Run `agentos note "$ARGUMENTS"` (quote the text exactly as given). Confirm the note id back to the user in one short line, then continue whatever you were doing before — the note will be delivered on your next review pass; do not act on it now.
